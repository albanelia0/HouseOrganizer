import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  logoContainer: {
    flexDirection: "row",
  },
  logoText: {
    color: "#56487C",
    width: 110,
    fontSize: 16,
    fontWeight: "bold"
  },
});